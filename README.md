# fs

A cross platform `fs` library for [lde](https://lde.sh).

It allows you to do things from recursively walk a directory, to set file permissions and create symlinks.

## Usage

```
lde add fs --git https://github.com/lde-org/fs
```
